type User @model @auth(rules: [{allow: public}]) {
  id: ID!
  firstName: String
  lastName: String
  email: String
  phone: String
  profilePhoto: String
  deviceToken: String
  venmo: String
  isMaster: Boolean
  isLive: Boolean
}
 